@page
@model W5L2.Pages.Users.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserA[0].FName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserA[0].LName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserB[0].title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserB[0].Biography)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserA[0].Age)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.UserA) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserB.title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserB.Biography)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
